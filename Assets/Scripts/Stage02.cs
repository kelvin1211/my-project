using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Stage02 : GameManager
{
    // Start is called before the first frame update
    void Awake()
    {
        //StartCoroutine(FadeIn());
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
